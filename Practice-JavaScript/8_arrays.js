// Step 8: Arrays Practice

let fruits = ["apple", "banana", "cherry"];
console.log(fruits[0]);

// Array methods
fruits.push("mango");
console.log(fruits);
fruits.pop();
console.log(fruits);

// Loop through array
for (let fruit of fruits) {
  console.log(fruit);
}

// Practice:
// 1. Create an array of 5 numbers.
// 2. Find the sum using a loop.
// 3. Use push and pop to add/remove elements.
