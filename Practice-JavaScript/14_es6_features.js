// Step 14: ES6 Features Practice

const PI = 3.14;
let radius = 5;
console.log(`Area: ${PI * radius * radius}`);

const add = (a, b) => a + b;
console.log(add(5, 3));

let person = { name: "John", age: 25 };
let { name, age } = person;
console.log(name, age);

// Practice:
// 1. Write an arrow function for multiplication.
// 2. Use template literals to print a greeting.
// 3. Use destructuring on an array.
